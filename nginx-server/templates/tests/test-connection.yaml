apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-server.fullname" . }}-test-connection"
  labels:
{{ include "nginx-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "nginx-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
